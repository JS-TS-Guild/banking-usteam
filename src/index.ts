require('module-alias/register');
// → create a Bank class

// add the attributes - id, name

// make a create() method which creates a bank

// make a createAccount() method which takes balance as an argument

// make a getId() method which gets the id of the bank, for example 1

// → create a User class

// add the attributes - id, name(take it as an argument)

// make a create() method which accepts name and bankAccountId as arguments

// → create an BankAccount class

// → create a TransactionService